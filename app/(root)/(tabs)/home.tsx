import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native";

const Home = () => {
  return (
    <SafeAreaView>
      <Text>Hello contact adjs Mastery Pro</Text>
    </SafeAreaView>
  );
};

export default Home;
